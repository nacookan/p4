# このファイルを .env としてコピーし、値を設定してください。
# .env 自体はコミットしないでください（.gitignoreで除外済み）。
#
# Dropbox App Console (https://www.dropbox.com/developers/apps) で作成した
# 「Scoped access」「App folder」タイプのアプリのApp Key。
# 秘密情報ではありません（PKCEフローの公開クライアントIDのため）。
DROPBOX_APP_KEY=YOUR_DROPBOX_APP_KEY_HERE
